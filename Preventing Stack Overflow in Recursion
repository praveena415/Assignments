let factorial=(num)=>{
  if(num<0 || isNaN(num)){
    return `invalid Input`
  }
  else if(num==1 || num==0){
    return 1
  }
  else{
    return num*factorial(num-1)
  }
}
console.log(factorial(5));
